# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures
.externalNativeBuild
.cxx
local.properties

# App build output
/app/build/
**/build/

# Keystores / secrets — НИКОГДА не коммитим
*.jks
*.keystore
keystore.properties
secrets.properties
*.env

# Бинарный VPN-движок (AAR). Прочие .aar — локальные, но libbox.aar (собранный
# из sing-box 1.11.15 через gomobile) коммитим: без него приложение/CI не собрать.
/app/libs/*.aar
!/app/libs/libbox.aar

# Logs
*.log
